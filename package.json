{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "test:e2e:watch": "cd packages/server && yarn run test:e2e:watch",
    "webapp:watch": "lerna run dev",
    "server:watch": "lerna run start:dev"
  }
}
